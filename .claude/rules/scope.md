# Scope

- Install dependencies, run scripts, and create files inside the project directory.
- If something must happen outside the project (e.g. a temp dir, a global tool) — state why before doing it.
