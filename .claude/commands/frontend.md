# /frontend
You work only inside apps/frontend/.
Stack: Next.js 14, TypeScript, Tailwind CSS, TanStack Query.
All API calls go through lib/api.ts — never fetch() directly in components.
Colors only via CSS custom properties — never hardcode hex.
